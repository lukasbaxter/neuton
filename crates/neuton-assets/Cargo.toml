[package]
name = "neuton-assets"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
png = "0.18.1"
serde_json = "1"
zip = { version = "6", default-features = false, features = ["deflate"] }

[dev-dependencies]
neuton-blocks = { path = "../neuton-blocks" }
